class AddressDTO {
  constructor(houseNo, street, state, district, pinCode, mobileNo) {
    this.houseNo = houseNo;
    this.street = street;
    this.state = state;
    this.district = district;
    this.pinCode = pinCode;
    this.mobileNo = mobileNo;
  }
}

export default AddressDTO;
